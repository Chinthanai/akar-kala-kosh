# Roadmap

- [x] Import project from GitHub into Lovable.
- [x] Restore missing project images (regenerated locally, wired into site data).
- [x] List all current failing SEO findings — none failing; robots, sitemap, and page basics all pass.
- [x] Fix every actionable failing SEO finding — nothing actionable remained.
- [x] Put the Google site verification code in the shared head so every page serves it (new code replaces the stale one).
- [ ] Click Verify in Google Search Console and submit the sitemap — blocked: needs your Search Console account; only you can verify.
- [x] Move canonical URLs, Open Graph URLs, social images, business schema, and sitemap references to pyararchitects.com.
